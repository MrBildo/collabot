# Slack bot token (xoxb-...)
SLACK_BOT_TOKEN=

# Slack app-level token for Socket Mode (xapp-...)
SLACK_APP_TOKEN=

# Path to the installed claude.exe binary.
# Required on Windows — the SDK's bundled binary has a git-bash path resolution
# bug that breaks subprocess spawning. Use the installed binary instead.
# Find yours with: where claude
CLAUDE_EXECUTABLE_PATH=

# Agent SDK defaults (overridable per-dispatch)
AGENT_MAX_TURNS=10
AGENT_MAX_BUDGET_USD=1.00

# Windows only — path to Git bash.exe with BACKSLASHES.
# Required because the SDK's git-bash auto-detection resolves to the wrong path.
# Non-Windows: leave blank or omit.
# Example: C:\Program Files\Git\bin\bash.exe
CLAUDE_CODE_GIT_BASH_PATH=

# Log level — controls harness console output verbosity.
#   minimal  = startup, errors, shutdown only (headless/production)
#   debug    = default — agent lifecycle, text, thinking, tool summaries
#   verbose  = everything — full tool input, token counts, heartbeat
# Omit or leave blank for "debug". HARNESS_VERBOSE=true is backward-compat for "verbose".
HARNESS_LOG_LEVEL=
